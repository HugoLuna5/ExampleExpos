<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lunainc.com.mx.exampleexpos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.etiquetas.ejercicio.SecondActivityEtiquetas"></activity>
        <activity android:name=".ui.etiquetas.ejercicio.EjercicioEtiquetaActivity" />
        <activity android:name=".ui.etiquetas.ejemplos.EjemploEtiquetaActivity" />
        <activity android:name=".ui.botones.ejercicio.SecondActivity" />
        <activity android:name=".ui.botones.ejercicio.FirstActivity" />
        <activity
            android:name=".ui.botones.ejemplo.EjemploBotonesActivity"
            android:label="@string/title_activity_ejemplo_botones" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>